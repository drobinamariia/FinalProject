{
  "name": "sharename-fullstack",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd sharename && python manage.py runserver\" \"cd frontend && npm start\"",
    "install-all": "npm install && pip install -r requirements.txt && cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
